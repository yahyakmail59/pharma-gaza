# دليل الرفع — PharmaGaza SaaS

اتبع الخطوات بالترتيب. مدة التنفيذ الأولى ٢٠–٣٠ دقيقة.

---

## ملفات المشروع

```
pharma-saas/
├── index.html              واجهة الصيدلية
├── admin.html              لوحة إدارة المنصة
├── app.js                  منطق التطبيق
├── admin.js                منطق لوحة المدير
├── app.css                 التنسيق
├── sw.js                   Service Worker (يعمل بلا إنترنت)
├── manifest.webmanifest    تعريف التطبيق للتثبيت
├── icon.svg                الأيقونة
├── _headers                رؤوس أمان Netlify
├── robots.txt
├── vendor/
│   ├── dexie.min.js        4.4.4 — مستضافة محلياً
│   └── html5-qrcode.min.js 2.3.8 — تُحمَّل عند أول مسح فقط
├── worker/                 ← لا تُرفع على Netlify
│   ├── worker.js
│   ├── schema.sql
│   └── reset-old-schema.sql
├── dev-server.js           للتجربة المحلية فقط
└── _backup_2026-08-16/     نسختك القديمة — احتفظ بها
```

---

## الخطوة ١ — قاعدة البيانات (Cloudflare D1)

1. افتح **Cloudflare Dashboard → Workers & Pages → D1**.
2. إن لم تكن قاعدة `pharma-db` موجودة: **Create database** باسم `pharma-db`.
3. افتح تبويب **Console**.
4. **إن كانت القاعدة تحوي جداول قديمة من النسخة السابقة:**
   الصق محتوى `worker/reset-old-schema.sql` واضغط Apply.
   ⚠️ هذا يحذف البيانات التجريبية. لو كانت لديك بيانات صيدلية حقيقية، توقّف واطلب سكربت ترحيل.
5. الصق محتوى `worker/schema.sql` كاملاً واضغط **Apply**.

**لماذا الحذف؟** المخطط القديم كان يخزّن `users.pin` نصاً صريحاً والمبالغ بأرقام عشرية. الجديد يستخدم `pin_hash` + `pin_salt` وأعداداً صحيحة. وأمر `CREATE TABLE IF NOT EXISTS` لا يعدّل جدولاً قائماً.

---

## الخطوة ٢ — السيرفر (Cloudflare Worker)

1. **Workers & Pages → Create → Create Worker**، سمّه `pharma-sync-api` ثم **Deploy**.
2. اضغط **Edit code**، امسح كل شيء، والصق محتوى `worker/worker.js`. ثم **Save and deploy**.
3. **Settings → Bindings → Add binding:**
   - النوع: **D1 database**
   - Variable name: `DB` (بحروف كبيرة، بالضبط)
   - Database: `pharma-db`
4. **Settings → Variables and Secrets → Add:**

   | الاسم | النوع | القيمة |
   |---|---|---|
   | `ADMIN_PASSWORD` | **Secret** | كلمة مرور قوية تختارها (١٦ حرفاً فأكثر) |
   | `ALLOWED_ORIGINS` | Text | يُضبط في الخطوة ٥ |

5. **Save and deploy**، وانسخ رابط الـ Worker.

> **مهم:** لا يوجد أي سر داخل `worker.js`. لو ظهر خطأ `1102 CPU exceeded` عند تسجيل الدخول على الخطة المجانية، افتح `worker.js` وغيّر `PBKDF2_ITER` من `50000` إلى `10000`.

---

## الخطوة ٣ — ضبط رابط السيرفر في الملفات

استبدل الرابط القديم برابطك في **ثلاثة مواضع**:

| الملف | السطر |
|---|---|
| `app.js` | `WORKER_URL:` داخل `CONFIG` (قرب أعلى الملف) |
| `admin.js` | `const WORKER_URL =` (السطر ١٣ تقريباً) |
| `_headers` | داخل `connect-src` في سطر `Content-Security-Policy` |

> إن نسيت `_headers` ستُحجب المزامنة، وسيظهر خطأ CSP في طرفية المتصفح.

---

## الخطوة ٤ — الرفع على Netlify

**أول مرة:**
1. افتح <https://app.netlify.com/drop>
2. اسحب مجلد `pharma-saas` كاملاً وأفلته.
3. انسخ الرابط الناتج.

**للتحديث لاحقاً — لا تنشئ مشروعاً جديداً:**
افتح موقعك → تبويب **Deploys** → اسحب المجلد وأفلته في منطقة السحب أسفل الصفحة. نفس الرابط يبقى.

> مجلد `worker/` و`dev-server.js` و`_backup_*` غير ضارة لو رُفعت (ملفات نصية لا تُنفَّذ)، لكن الأنظف حذفها من نسخة الرفع.

---

## الخطوة ٥ — إغلاق CORS

ارجع إلى الـ Worker → **Settings → Variables** واضبط:

```
ALLOWED_ORIGINS = https://اسم-موقعك.netlify.app
```

بدون هذا يقبل السيرفر الطلبات من أي موقع. **Save and deploy**.

---

## الخطوة ٦ — التشغيل

1. افتح `https://موقعك.netlify.app/admin.html`
2. أدخل `ADMIN_PASSWORD` الذي ضبطته.
3. أنشئ صيدلية: الرمز `PHM-001`، ورقم سري للمالك من ٤ إلى ٦ أرقام (تجنّب `1234` و`0000` — النظام يرفضها).
4. افتح `https://موقعك.netlify.app/` وسجّل الدخول بالرمز والرقم السري.
5. جرّب: أضف صنفاً ← أضف دفعة ← ابحث ← بِع ← افتح التقارير.

---

## الاختبار المحلي (اختياري)

```bash
node dev-server.js
```
ثم افتح <http://localhost:4899>. لن يعمل الفتح المباشر بنقر `index.html` لأن Service Worker وبعض الميزات تتطلب `http://`.

---

## ما تغيّر عن النسخة السابقة

**الأمان**
- حُذف `ADMIN_SECRET` من المتصفح نهائياً. لوحة المدير تعمل بتوكن من السيرفر ينتهي بعد ٨ ساعات.
- الأرقام السرية تُجزَّأ بـ PBKDF2 بملح فريد. لا يوجد عمود نصي لها لا محلياً ولا سحابياً، ولا تُعرض في أي شاشة.
- كل طلبات المزامنة تتطلب توكناً، والسيرفر **يستنتج الصيدلية من التوكن** ويتجاهل ما يرسله المتصفح — هذا ما يمنع صيدلية من قراءة بيانات أخرى.
- قفل تصاعدي بعد ٥ محاولات فاشلة (دقيقة ← ٥ ← ١٥ ← ساعة).
- لا يُبنى أي HTML من بيانات المستخدم؛ اسم دواء يحوي كوداً يُعرض نصاً (مُختبَر).
- CSP ورؤوس أمان في `_headers`، وقفل تلقائي بعد ١٠ دقائق خمول.
- إيقاف الاشتراك يُلغي جلسات الصيدلية فوراً.

**صحة البيانات**
- كل المبالغ أعداد صحيحة بالأغورة (`0.1 + 0.2 = 0.30` بالضبط).
- الديون تعمل فعلاً: يُحفظ `customer_id` ويزداد دين الزبون، مع سداد جزئي مسجَّل.
- منع بيع الدفعات المنتهية، وعرض الصلاحية في نتائج البحث مع ترتيب FEFO.
- المزامنة تنقل **حركات** لا أرصدة، فلا يضيع بيع جهاز آخر.
- كل الجداول تُزامَن، والفواتير تحمل الأصناف والبائع ونوع الدفع.
- إبطال فاتورة بقيد عكسي يعيد المخزون ويخصم الدين.
- سجل تدقيق لكل عملية حساسة، وتسوية المخزون تتطلب سبباً.

**الأداء والواجهة**
- فهارس مركّبة `[pharmacy_id+created_at]` — لوحة التحكم لم تعد تمسح كل الفواتير.
- ترقيم صفحات المخزون (٥٠) وبحث بـ debounce.
- Service Worker بالشبكة أولاً: يعمل بلا إنترنت، **وتصل التحديثات فوراً**.
- شريط سفلي على الجوال، والجداول تتحول لبطاقات (لا تمرير أفقي).
- تباين مصحّح ليجتاز WCAG AA، والتكبير مسموح مجدداً.
- تدفق لوحة مفاتيح كامل: Enter لمسدس الباركود، الأسهم، F2/F4/F9.
- نسخة احتياطية **مشفّرة فعلياً** (AES-GCM) وخاصة بصيدلية واحدة وبلا أرقام سرية.
- استيراد تلقائي وآمن لبيانات النسخة القديمة عند أول دخول.

---

## نقاط تستحق الانتباه

- **الرقم السري من ٤ خانات يبقى ضعيفاً بطبيعته** (١٠٬٠٠٠ احتمال). الحماية الحقيقية هي القفل التصاعدي. للمالك استخدم ٦ خانات.
- **المزامنة الأولى على جهاز جديد تتطلب إنترنت.** بعدها يعمل بلا شبكة.
- **لا يوجد Git بعد.** شغّل `git init` والتزم بنسخة — لا تراجع بدونه.
- **جرّب على جهاز واحد أسبوعاً** قبل تسليمه لصيدلية حقيقية.
